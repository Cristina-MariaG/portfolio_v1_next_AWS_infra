'use client';

import Footer from '@/components/common/Footer';

export default function LandingPage() {
  return (
    <div id='contact' className='h-screen flex items-center justify-center'>
      <Footer timerValue={200} />
    </div>
  );
}
